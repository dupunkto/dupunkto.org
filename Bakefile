#!/usr/bin/env bash

SRC=src
DOMAIN=dupunkto.org

# Skip `tools` directory on remote
DEPLOY="$DEPLOY --exclude tools --exclude press --exclude reader"

deploy() {
  $DEPLOY $DIST/ s11:domains/$DOMAIN/public_html
  $DEPLOY $DIST/ nov:/volume1/www/$DOMAIN
}

serve() {
  cd $SRC
  waiter --dev
}